import 'package:flutter/material.dart';

class ControlMuestrasPage extends StatelessWidget {
  const ControlMuestrasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Control de Muestras')),
      body: const Center(child: Text('Welcome to Control de Muestras Page!')),
    );
  }
}
